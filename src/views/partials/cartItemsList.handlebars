
<ul class="cartItemsList" id="cartItemsList">
  {{#if items.length}}
  {{#each items as |item|}}
  <li class="productCard">
    <img src="/images/{{item.product.thumbnails.[0]}}" alt="{{item.product.title}}">
    <div>
      <h3 class="title">{{item.product.title}}</h3>
      <p class="description">{{item.product.description}}</p>
      <p>Unit Price: <b>${{item.product.price}}</b></p>

    </div>
    <div>
      <p>Quantity: <b>{{item.quantity}}</b></p>
      <p>Total Price: <b>${{item.totalPrice}}</b></p>
    </div>
  </li>
  {{/each}}

    <form action="/checkout" method="GET">
    <button type="submit" class="pastel-bg">Comprar</button>
  </form>
  
  {{else}}
  <h2>The cart is empty!</h2>
  {{/if}}
</ul>

{{!-- <ul class="cartItemsList" id="cartItemsList">
  {{#if items.length}}
  {{#each items as |item index|}}
  <li class="productCard">
    <img src="/images/{{item.product.thumbnails.[0]}}" alt="{{item.product.title}}">
    <div>
      <h3 class="title">{{item.product.title}}</h3>
      <p class="description">{{item.product.description}}</p>
      <p>Unit Price: <b>${{item.product.price}}</b></p>
    </div>
    <div>
      <p>Quantity: <b>{{item.quantity}}</b></p>
      <p>Total Price: <b>${{item.totalPrice}}</b></p>
  <form action="/api/carts/{{cartId}}/product/{{productId}}" method="POST">
  <input type="hidden" name="_method" value="DELETE">
  <button type="submit" class="danger-bg">Quitar Producto</button>
</form>

    </div>
  </li>
  {{/each}}
  <form action="/checkout" method="GET">
    <button type="submit" class="pastel-bg">Comprar</button>
  </form>
  {{else}}
  <h2>The cart is empty!</h2>
  {{/if}}
</ul> --}}